"""Drawing the results of the core, for the fronts that want it (the `render` extra :
`pip install kpiten-core[render]`) : `plotly` for the graphs, `gtable` for the tables.

The core itself computes data only (`charts.Chart`, dataframes) : a front may draw them
with another library, without these modules.
"""
