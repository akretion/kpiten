"""The syntax of the KPI (version 2) : in kpiten-spec, re-exported here.

The schema, the validation of a definition and the header of a SQL tile are in the
package `kpiten_spec` (standard library only), which the Odoo modules use without the
rest of kpiten-core.
"""

from kpiten_spec.spec import *  # noqa: F401,F403
