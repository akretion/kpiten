<odoo>
    <!-- the orders and their lines do not call the date the same : a list ; after the
         panels of kpiten_kpi (Sales first when both are installed) -->
    <record model="kt.panel" id="panel_essential">
        <field name="name">Essentials</field>
        <field name="sequence">100</field>
        <field
            name="filter_config"
        >{"date": {"field": ["date_order", "order_id.date_order"]}, "dimensions": [{"name": "user_id", "label": "Salesperson"}, {"name": "partner_id", "label": "Customer"}]}</field>
    </record>
</odoo>
