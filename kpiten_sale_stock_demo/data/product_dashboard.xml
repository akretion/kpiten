<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">
    <!-- The "Product" dashboard of Odoo (spreadsheet_dashboard_sale) : best sellers
        and best categories over the order lines, by revenue and by units sold. The
        Period filter is on the date of the order (order_id.date_order, see
        models/kt.py). -->

    <record id="panel_product" model="kt.panel">
        <field name="name">Product</field>
        <field
            name="filter_config"
        >{"date": {"field": "order_id.date_order"}, "dimensions": [{"name": "product_id", "label": "Product"}, {"name": "product_id.categ_id.complete_name", "label": "Category"}]}</field>
    </record>

    <!-- the two scorecards : the name of the best, its units sold under it -->
    <record id="product_best_seller_card" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">card</field>
        <field name="name">Best Seller</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">1</field>
        <field name="definition">where = "state not in ('draft', 'sent', 'cancel')"
best = "product_id"
measure = "price_subtotal"
detail = "product_uom_qty"
detail_label = "sold"
</field>
    </record>

    <record id="product_best_category_card" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">card</field>
        <field name="name">Best Category</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">2</field>
        <field name="definition">where = "state not in ('draft', 'sent', 'cancel')"
best = "product_id.categ_id.complete_name"
measure = "price_subtotal"
detail = "product_uom_qty"
detail_label = "sold"
</field>
    </record>

    <!-- Best Sellers by Revenue / by Units Sold : a bar per product, biggest first -->
    <record id="product_best_sellers_revenue_graph" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">graph</field>
        <field name="name">Best Sellers by Revenue</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">3</field>
        <field name="col_span">3</field>
        <field name="tile_height">340</field>
        <field name="definition">graph_type = "bar"
where = "state not in ('draft', 'sent', 'cancel')"
[x]
name = "product_id"
aggregation = "none"
[y]
name = "price_subtotal"
aggregation = "sum"
</field>
    </record>

    <record id="product_best_sellers_units_graph" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">graph</field>
        <field name="name">Best Sellers by Units Sold</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">4</field>
        <field name="col_span">3</field>
        <field name="tile_height">340</field>
        <field name="definition">graph_type = "bar"
where = "state not in ('draft', 'sent', 'cancel')"
[x]
name = "product_id"
aggregation = "none"
[y]
name = "product_uom_qty"
aggregation = "sum"
</field>
    </record>

    <!-- Best Selling Products (a link to each product) / Best Selling Categories -->
    <record id="product_best_selling_products_table" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">data</field>
        <field name="name">Best Selling Products</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("product_id").is_not_null())
# product_id_ (the id) is kept to build the link
d_next = d_next.group_by(["product_id", "product_id_"]).agg([pl.col("product_uom_qty").sum().alias("Units"), pl.col("price_subtotal").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "product_id"], descending=[True, False]).head(10)
# a "[label](url)" cell is drawn as a link to Odoo : odoo_url is the base url, `product_id_` the id
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("product_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/product.product/"), pl.col("product_id_").cast(pl.String), pl.lit(")")]).alias("Product"),
    pl.col("Units"),
    pl.col("Revenue"),
    # hidden column (`__` prefix) : a click on the row hands it to the drill-down as key['product_id_']
    pl.col("product_id_").alias("__product_id_"),
])</field>
        <field name="drill_definition">d_next = d
# the order lines of the clicked product, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("product_id_") == key["product_id_"]))
d_next = d_next.sort(["order_id.date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("order_id.date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("order_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("order_id_").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("order_partner_id").alias("Customer"),
    pl.col("product_id").alias("Product"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_subtotal").alias("Revenue"),
])</field>
    </record>

    <record id="product_best_selling_categories_table" model="kt.dataset.line">
        <field name="dataset_id" ref="kpiten_demo.kpiten_sales_lines" />
        <field name="kind">data</field>
        <field name="name">Best Selling Categories</field>
        <field name="panel_id" ref="panel_product" />
        <field name="sequence">6</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("product_id.categ_id.complete_name").is_not_null())
# dotted name : a relational path declared for the dataset (line &gt; product &gt; category)
d_next = d_next.group_by("product_id.categ_id.complete_name").agg([pl.col("product_uom_qty").sum().alias("Units"), pl.col("price_subtotal").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "product_id.categ_id.complete_name"], descending=[True, False]).head(10)
d_next = d_next.rename({"product_id.categ_id.complete_name": "Category"})
# hidden column : the category name is the key
d_next = d_next.with_columns(pl.col("Category").alias("__category"))</field>
        <field name="drill_definition">d_next = d
# the order lines of the clicked category, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("product_id.categ_id.complete_name") == key["category"]))
d_next = d_next.sort(["order_id.date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("order_id.date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("order_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("order_id_").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("order_partner_id").alias("Customer"),
    pl.col("product_id").alias("Product"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_subtotal").alias("Revenue"),
])</field>
    </record>
</odoo>
