<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <!-- Sales KPI cards, the four scorecards of the Odoo "Sales" dashboard
        (spreadsheet_dashboard_sale) : same states and measures, the period of
        the panel plays the role of Odoo's Period filter, and `compare = true`
        gives the change against the period before ("since last period"). On the demo data
        they give the same figures as Odoo's dashboard for any period. -->

    <record id="sale_quotations_card" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">card</field>
        <field name="name">Quotations</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">20</field>
        <field name="definition">version = 2
where = "state in ('draft', 'sent')"
compare = true
</field>
    </record>

    <record id="sale_orders_card" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">card</field>
        <field name="name">Orders</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">21</field>
        <field name="definition">version = 2
where = "state = 'sale'"
compare = true
</field>
    </record>

    <record id="sale_revenue_card" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">card</field>
        <field name="name">Revenue</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">22</field>
        <field name="definition">version = 2
where = "state = 'sale'"
measure = "amount_untaxed"
aggregation = "sum"
unit = "currency"
compare = true
</field>
    </record>

    <record id="sale_avg_order_card" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">card</field>
        <field name="name">Average Order</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">23</field>
        <field name="definition">version = 2
where = "state = 'sale'"
measure = "amount_untaxed"
aggregation = "mean"
unit = "currency"
decimals = 0
compare = true
</field>
    </record>

    <!-- the "Monthly sales" chart of the same dashboard : revenue of the
        confirmed orders per month, as a filled line, full width -->
    <record id="sale_monthly_sales_graph" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">graph</field>
        <field name="name">Monthly sales</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">1</field>
        <field name="col_span">3</field>
        <field name="tile_height">340</field>
        <field name="definition">version = 2
type = "area"
where = "state not in ('draft', 'cancel', 'sent')"
by = "date_order"
grain = "month"
measure = "amount_untaxed"
aggregation = "sum"
</field>
    </record>

    <!-- "Top Sales Orders" of the same dashboard : the ten confirmed orders
        with the biggest untaxed amount (then invoice status, then the default
        order of sale.order), with the panel period. The customer is a link to
        the order in Odoo : a cell "[label](url)" is drawn as a link, `odoo_url`
        is the base url of Odoo given to the snippet -->
    <record id="sale_top_orders_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Sales Orders</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders only
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]))
# the last sort keys only break ties : stable order
d_next = d_next.sort(["amount_untaxed", "invoice_status", "date_order", "id"], descending=[True, False, True, True]).head(10)
# a "[label](url)" cell is drawn as a link to Odoo : odoo_url is the base url, `id` the id
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("partner_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("id").cast(pl.String), pl.lit(")")]).alias("Customer"),
    pl.col("user_id").alias("Salesperson"),
    pl.col("amount_untaxed").alias("Revenue"),
    # hidden column (`__` prefix) : a click on the row hands it to the drill-down as key['id']
    pl.col("id").alias("__id"),
])</field>
        <field name="drill_definition">d_next = d
# the lines of the clicked order : `tables` holds the other tables of the store, filtered like the tile
d_next = tables["sale.order.line"].filter(pl.col("order_id_") == key["id"]).sort("id")
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("product_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/product.product/"), pl.col("product_id_").cast(pl.String), pl.lit(")")]).alias("Product"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_unit").alias("Unit price"),
    pl.col("price_subtotal").alias("Subtotal"),
])</field>
    </record>

    <!-- "Top Quotations" of the same dashboard : the ten quotations (draft or
        sent) with the biggest untaxed amount, then the default order of
        sale.order, with the panel period -->
    <record id="sale_top_quotations_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Quotations</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# open quotations
d_next = d_next.filter(pl.col("state").is_in(["draft", "sent"]))
d_next = d_next.sort(["amount_untaxed", "date_order", "id"], descending=[True, True, True]).head(10)
# a "[label](url)" cell is drawn as a link to Odoo : odoo_url is the base url, `id` the id
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("partner_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("id").cast(pl.String), pl.lit(")")]).alias("Customer"),
    pl.col("user_id").alias("Salesperson"),
    pl.col("amount_untaxed").alias("Revenue"),
    # hidden column (`__` prefix) : a click on the row hands it to the drill-down as key['id']
    pl.col("id").alias("__id"),
])</field>
        <field name="drill_definition">d_next = d
# the lines of the clicked quotation (another table of the store : `tables`)
d_next = tables["sale.order.line"].filter(pl.col("order_id_") == key["id"]).sort("id")
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("product_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/product.product/"), pl.col("product_id_").cast(pl.String), pl.lit(")")]).alias("Product"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_unit").alias("Unit price"),
    pl.col("price_subtotal").alias("Subtotal"),
])</field>
    </record>

    <!-- "Top Products" of the same dashboard : per product, the number of
        confirmed orders it is in and its revenue, the ten best. On the order
        lines : the period filter reaches them through `order_id.date_order`
        (see models/kt.py and the panel filter_config). The product is a link to
        its form in Odoo (see the Top Sales Orders table) -->
    <record id="sale_top_products_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales_lines" />
        <field name="kind">data</field>
        <field name="name">Top Products</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("product_id").is_not_null())
# n_unique : an order has several lines ; product_id_ (the id) is kept to build the link
d_next = d_next.group_by(["product_id", "product_id_"]).agg([pl.col("order_id_").n_unique().alias("Orders"), pl.col("price_subtotal").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "product_id"], descending=[True, False]).head(10)
# a "[label](url)" cell is drawn as a link to Odoo : odoo_url is the base url, `product_id_` the id
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("product_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/product.product/"), pl.col("product_id_").cast(pl.String), pl.lit(")")]).alias("Product"),
    pl.col("Orders"),
    pl.col("Revenue"),
    # hidden column (`__` prefix) : a click on the row hands it to the drill-down as key["product_id_"]
    pl.col("product_id_").alias("__product_id_"),
])</field>
        <field name="drill_definition">d_next = d
# key : the hidden columns of the clicked row. Same rows as the tile : confirmed lines of that product
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("product_id_") == key["product_id_"]))
d_next = d_next.sort(["order_id.date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("order_id.date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("order_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("order_id_").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("order_partner_id").alias("Customer"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_subtotal").alias("Revenue"),
])</field>
    </record>

    <!-- "Top Salespeople" of the same dashboard : per salesperson, the number of
        confirmed orders and their revenue, the ten best, with the panel period -->
    <record id="sale_top_salespeople_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Salespeople</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders that have a salesperson
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("user_id").is_not_null())
d_next = d_next.group_by(["user_id", "user_id_"]).agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "user_id"], descending=[True, False]).head(10)
# hidden column : the id of the salesperson
d_next = d_next.rename({"user_id": "Salesperson", "user_id_": "__user_id_"})</field>
        <field name="drill_definition">d_next = d
# the orders of the clicked salesperson, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("user_id_") == key["user_id_"]))
d_next = d_next.sort(["date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("name"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("id").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("partner_id").alias("Customer"),
    pl.col("user_id").alias("Salesperson"),
    pl.col("amount_untaxed").alias("Revenue"),
])</field>
    </record>

    <!-- "Top Customers" of the same dashboard : per customer, the confirmed orders and their revenue, the ten best. The customer is a link to its form in Odoo -->
    <record id="sale_top_customers_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Customers</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("partner_id").is_not_null())
# partner_id_ (the id) is kept to build the link
d_next = d_next.group_by(["partner_id", "partner_id_"]).agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "partner_id"], descending=[True, False]).head(10)
# a "[label](url)" cell is drawn as a link to Odoo : odoo_url is the base url, `partner_id_` the id
d_next = d_next.select([
    pl.concat_str([pl.lit("["), pl.col("partner_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/res.partner/"), pl.col("partner_id_").cast(pl.String), pl.lit(")")]).alias("Customer"),
    pl.col("Orders"),
    pl.col("Revenue"),
    # hidden column (`__` prefix) : a click on the row hands it to the drill-down as key['partner_id_']
    pl.col("partner_id_").alias("__partner_id_"),
])</field>
        <field name="drill_definition">d_next = d
# the orders of the clicked customer, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("partner_id_") == key["partner_id_"]))
d_next = d_next.sort(["date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("name"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("id").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("partner_id").alias("Customer"),
    pl.col("user_id").alias("Salesperson"),
    pl.col("amount_untaxed").alias("Revenue"),
])</field>
    </record>

    <!-- "Top Categories" : per product category, the orders it is in and its revenue, on the order lines. The category is shown with its path, as Odoo does -->
    <record id="sale_top_categories_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales_lines" />
        <field name="kind">data</field>
        <field name="name">Top Categories</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("product_id.categ_id.complete_name").is_not_null())
# dotted name : a relational path declared for the dataset (line &gt; product &gt; category) ; n_unique : an order has several lines
d_next = d_next.group_by("product_id.categ_id.complete_name").agg([pl.col("order_id_").n_unique().alias("Orders"), pl.col("price_subtotal").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "product_id.categ_id.complete_name"], descending=[True, False]).head(10)
d_next = d_next.rename({"product_id.categ_id.complete_name": "Category"})
# hidden column : the category name is the key
d_next = d_next.with_columns(pl.col("Category").alias("__category"))</field>
        <field name="drill_definition">d_next = d
# the order lines of the clicked category, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("product_id.categ_id.complete_name") == key["category"]))
d_next = d_next.sort(["order_id.date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("order_id.date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("order_id"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("order_id_").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("order_partner_id").alias("Customer"),
    pl.col("product_id").alias("Product"),
    pl.col("product_uom_qty").alias("Quantity"),
    pl.col("price_subtotal").alias("Revenue"),
])</field>
    </record>

    <!-- "Top Sales Teams" : per sales team, the confirmed orders and their revenue -->
    <record id="sale_top_sales_teams_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Sales Teams</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders that have a team
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("team_id").is_not_null())
d_next = d_next.group_by("team_id").agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "team_id"], descending=[True, False]).head(10)
d_next = d_next.rename({"team_id": "Sales Team"})</field>
    </record>

    <!-- "Top Sources" : per campaign source, the confirmed orders and their revenue (empty while no order has a source) -->
    <record id="sale_top_sources_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Sources</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders that have a source
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("source_id").is_not_null())
d_next = d_next.group_by("source_id").agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "source_id"], descending=[True, False]).head(10)
d_next = d_next.rename({"source_id": "Source"})</field>
    </record>

    <!-- "Top Mediums" : per campaign medium, the confirmed orders and their revenue (empty while no order has a medium) -->
    <record id="sale_top_mediums_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Mediums</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders that have a medium
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("medium_id").is_not_null())
d_next = d_next.group_by("medium_id").agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "medium_id"], descending=[True, False]).head(10)
d_next = d_next.rename({"medium_id": "Medium"})</field>
    </record>

    <!-- "Top Countries" : per customer country, the confirmed orders and their revenue (empty while no customer has a country) -->
    <record id="sale_top_countries_table" model="kt.kpi">
        <field name="dataset_id" ref="kpiten_commercial_data.kpiten_sales" />
        <field name="kind">data</field>
        <field name="name">Top Countries</field>
        <field name="panel_id" ref="kpiten_commercial_data.panel_sale" />
        <field name="sequence">5</field>
        <field name="col_span">2</field>
        <field name="tile_height">380</field>
        <field name="definition">d_next = d
# d : the rows the user may read, already filtered by the panel period and dimensions ; line 1 must stay `out = in`
# confirmed orders
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; pl.col("partner_id.country_id").is_not_null())
# dotted name : a relational path declared for the dataset (order &gt; customer &gt; country)
d_next = d_next.group_by("partner_id.country_id").agg([pl.len().alias("Orders"), pl.col("amount_untaxed").sum().alias("Revenue")])
# the last sort keys only break ties : stable order
d_next = d_next.sort(["Revenue", "partner_id.country_id"], descending=[True, False]).head(10)
d_next = d_next.rename({"partner_id.country_id": "Country"})
# hidden column : the country name is the key
d_next = d_next.with_columns(pl.col("Country").alias("__country"))</field>
        <field name="drill_definition">d_next = d
# the orders of the clicked country, same rows as the tile
d_next = d_next.filter(~pl.col("state").is_in(["draft", "sent", "cancel"]) &amp; (pl.col("partner_id.country_id") == key["country"]))
d_next = d_next.sort(["date_order", "id"], descending=[True, True])
d_next = d_next.select([
    pl.col("date_order").alias("Date"),
    pl.concat_str([pl.lit("["), pl.col("name"), pl.lit("]("), pl.lit(odoo_url), pl.lit("/odoo/sale.order/"), pl.col("id").cast(pl.String), pl.lit(")")]).alias("Order"),
    pl.col("partner_id").alias("Customer"),
    pl.col("user_id").alias("Salesperson"),
    pl.col("amount_untaxed").alias("Revenue"),
])</field>
    </record>
</odoo>
